
增强版（无花瓣动画、透明界面、改进打字效果）

说明：
- 花瓣动画已移除，界面改为透明卡片，让背景图可视（把 background.jpg 放在仓库根目录并在首页填写文件名或上传本地背景）。
- 打字效果从逐字符改为逐行打字并带淡入，让页面更优雅不别扭。
- 歌词支持 LRC，音乐播放时会同步高亮并自动滚动。
- 相册为会话预览，不会写入 localStorage。

部署：把本文件夹上传到 GitHub 仓库根目录，启用 Pages（main branch / root），几分钟后访问。


-- 已集成 --
- background.jpg 已放入根目录（可以替换为你自己的背景图）
- lyrics.lrc 已作为示例放入根目录，上传你自己的 LRC 并在页面填写文件名即可同步歌词
- 诗句已调整为更古风的风格（如你所愿）
